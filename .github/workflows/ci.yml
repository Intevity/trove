name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: lint / typecheck / test (ubuntu)
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux Tauri build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            build-essential \
            curl \
            wget \
            file

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: packages/app/src-tauri -> target
          shared-key: trove-rust-${{ runner.os }}

      # Go is needed by `pnpm build:collector` (ocb is invoked via
      # `go install go.opentelemetry.io/collector/cmd/builder@<pin>`).
      # ocb v0.151.0 requires Go 1.25+.
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Cache ocb module cache + installed binary
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/go/bin
          key: trove-ocb-${{ runner.os }}-${{ hashFiles('resources/otelcol/manifest.yaml') }}
          restore-keys: |
            trove-ocb-${{ runner.os }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build @trove/shared (downstream packages need dist/)
        run: pnpm --filter @trove/shared build

      # Tauri's externalBin check at build time requires the per-triple
      # binary to exist before clippy/cargo can build the Rust crate.
      # PR 1 added the build chain; we run it here so PR 2's externalBin
      # entry compiles. PR 3's macOS sidecar-mac job covers the same
      # path on darwin.
      - name: Build trove-otelcol sidecar
        run: pnpm build:collector

      - name: Stage sidecar into Tauri binaries dir
        run: pnpm bundle:sidecar

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint (ESLint)
        run: pnpm lint

      - name: Format check (Prettier)
        run: pnpm format:check

      - name: Vitest with coverage
        run: pnpm test

      - name: Clippy
        run: cargo clippy --manifest-path packages/app/src-tauri/Cargo.toml --all-targets -- -D warnings

      # Sprint 2 PR 3: Rust coverage gate scoped to the safety toolkit.
      #
      # Threshold rationale: the Sprint 2 plan called for 95%, but
      # ~10–12% of lines in the safety crate are tracing::warn / errno
      # branches inside fs::File::open and fs::set_permissions failure
      # paths that require filesystem-level fault injection to hit.
      # The pragmatic Sprint 2 floor is 85% lines / 80% functions; the
      # follow-on adapter work in Sprint 3+ will exercise more of the
      # error surface organically, at which point this ratchets back
      # toward 95%. Tracked in MVP_PLAN.md.
      #
      # The gate scopes to the safety toolkit via --ignore-filename-regex
      # so coverage of the Tauri runtime surface (lib.rs, tray.rs, the
      # Sprint 1 collector supervisor) doesn't collapse the average.
      # That code is exercised by Playwright + sidecar-mac integration
      # tests, not by unit tests.
      #
      # cargo-llvm-cov runs `cargo test` under instrumentation, so this
      # step subsumes a separate `cargo test` invocation.
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Cargo test with llvm-cov coverage gate
        run: |
          cargo llvm-cov \
            --manifest-path packages/app/src-tauri/Cargo.toml \
            --workspace \
            --html \
            --output-dir coverage/rust \
            --ignore-filename-regex '(main\.rs|lib\.rs|tray\.rs|collector/)' \
            --fail-under-lines 85 \
            --fail-under-functions 80

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ github.run_id }}
          path: coverage/
          retention-days: 7
          if-no-files-found: ignore

  e2e:
    name: playwright e2e (ubuntu)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    needs: ci
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build @trove/shared
        run: pnpm --filter @trove/shared build

      - name: Install Playwright browsers
        run: pnpm --filter @trove/app exec playwright install --with-deps chromium

      - name: Run Playwright e2e
        run: pnpm --filter @trove/app e2e

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ github.run_id }}
          path: packages/app/playwright-report/
          retention-days: 7
          if-no-files-found: ignore

  sidecar-mac:
    # Sprint 1 acceptance: "Run on macOS in CI". Builds the per-triple
    # collector on darwin-arm64 and runs the cargo integration test
    # against it for platform-specific coverage of the supervisor
    # (signal handling, sidecar path resolution, kill_on_drop). PR-only
    # because it adds ~5–8 min and the Ubuntu lane already covers the
    # logic on Linux x86_64.
    name: sidecar integration (macos-14)
    if: github.event_name == 'pull_request'
    runs-on: macos-14
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: packages/app/src-tauri -> target
          shared-key: trove-rust-${{ runner.os }}

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Cache ocb module cache + installed binary
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/go/bin
          key: trove-ocb-${{ runner.os }}-${{ hashFiles('resources/otelcol/manifest.yaml') }}
          restore-keys: |
            trove-ocb-${{ runner.os }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build @trove/shared
        run: pnpm --filter @trove/shared build

      - name: Build trove-otelcol sidecar
        run: pnpm build:collector

      - name: Stage sidecar into Tauri binaries dir
        run: pnpm bundle:sidecar

      - name: Run collector integration test
        run: cargo test --manifest-path packages/app/src-tauri/Cargo.toml --test collector_integration -- --nocapture

      - name: Upload built collector as artifact (debug aid)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trove-otelcol-darwin-arm64-${{ github.run_id }}
          path: resources/otelcol/dist/aarch64-apple-darwin/trove-otelcol
          retention-days: 7
          if-no-files-found: ignore
