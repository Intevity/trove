<svg viewBox="0 0 1024 1024" width="1024" height="1024" xmlns="http://www.w3.org/2000/svg">
  <title>Trove tray-icon silhouette</title>
  <desc>Inverse of the app icon: an opaque rounded square with the T-monogram cut out as transparent space. The dynamic tinter at tray_icon_render.rs replaces opaque pixels with the health-state color (green/amber/red) while leaving the T cutout transparent — preserving both brand identity and the dashboard health contract.</desc>
  <defs>
    <mask id="t-cutout" maskUnits="userSpaceOnUse" x="0" y="0" width="1024" height="1024">
      <rect width="1024" height="1024" fill="black"/>
      <rect x="100" y="100" width="824" height="824" rx="180" fill="white"/>
      <rect x="212" y="212" width="600" height="160" rx="16" fill="black"/>
      <rect x="432" y="212" width="160" height="600" rx="16" fill="black"/>
    </mask>
  </defs>
  <rect width="1024" height="1024" fill="white" mask="url(#t-cutout)"/>
</svg>
